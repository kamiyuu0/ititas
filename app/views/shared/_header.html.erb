<header class="navbar bg-base-100 shadow-md px-4">
  <!-- 左：ロゴ or アイコン -->
  <div class="flex-1">
    <%= link_to current_user.present? ? mypage_path : root_path , class: "text-xl font-bold flex items-center gap-2" do %>
      🐣📆
      <span class="hidden sm:inline">いちたす</span>
    <% end %>
  </div>

  <!-- 右：デスクトップナビゲーション（md以上で表示） -->
  <div class="hidden md:flex flex-none space-x-2">
    <% if user_signed_in? %>
      <%= link_to "☁️ マイページ", mypage_path, class: "btn btn-ghost btn-sm" %>
      <%= link_to "📅 タスク作成", new_task_path, class: "btn btn-ghost btn-sm" %>
      <%= link_to "🌤 一覧ページ", tasks_path, class: "btn btn-ghost btn-sm" %>
      <%= link_to "サインアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-outline btn-sm" %>
    <% else %>
      <%= link_to "🌤 はじめる", new_user_registration_path, class: "btn btn-primary btn-sm" %>
      <%= link_to "🎯 ログイン", new_user_session_path, class: "btn btn-outline btn-sm" %>
    <% end %>
  </div>

  <!-- ハンバーガーメニュー（md未満で表示） -->
  <div class="md:hidden dropdown dropdown-end">
    <div tabindex="0" role="button" class="btn btn-ghost btn-circle">
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
      </svg>
    </div>
    <ul tabindex="0" class="dropdown-content menu p-2 shadow bg-base-100 rounded-box w-52 mt-3">
      <% if user_signed_in? %>
        <li><%= link_to "☁️ マイページ", mypage_path %></li>
        <li><%= link_to "📅 タスク作成", new_task_path %></li>
        <li><%= link_to "🌤 一覧ページ", tasks_path %></li>
        <li class="border-t pt-2 mt-2">
          <%= link_to "サインアウト", destroy_user_session_path, data: { turbo_method: :delete } %>
        </li>
      <% else %>
        <li><%= link_to "🌤 はじめる", new_user_registration_path %></li>
        <li><%= link_to "🎯 ログイン", new_user_session_path %></li>
      <% end %>
    </ul>
  </div>
</header>
